/*
 * Config
 */
define(['./Base'], function (Base) {
    console.log('config called');
    var config = {
		app_list: {
			'calculator': {
				subscriptions: [
					{
						channel: 'calculator', 
						modelTopics: ['calculate'],
						viewTopics: ['calculated']
					}
				]
			}
		}
	};
    var _Config = new Base(config);
    return _Config;
});