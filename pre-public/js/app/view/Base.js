/*
 * ViewBase
 */
define(function () {
    console.log('viewBase called');	
    function viewBase(id) {
        this.id = id;
    };
    viewBase.prototype = {
		// to do
    };
    return viewBase;
});
